import { RecordTypeShortPipe } from './record-type-short.pipe';

describe('RecordTypeShortPipe', () => {
  it('create an instance', () => {
    const pipe = new RecordTypeShortPipe();
    expect(pipe).toBeTruthy();
  });
});
