import { PerformanceTypePipe } from './performance-type.pipe';

describe('PerformanceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PerformanceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
